# 자문 반영 작업지시서 — 2026-09-09

사회복지학과 교수 자문에서 받은 답을 **어느 파일 어느 필드에 넣을지** 미리 정해 둔다.
자문 자리에서 이 문서를 열어 두고, 답을 들은 즉시 해당 칸을 채운다.

원칙: **답변은 전부 데이터로 들어간다. 코드를 고치는 항목은 아래 3절뿐이다.**

---

## 1. 답을 받으면 곧바로 값이 되는 것

### A-2. 재산의 소득환산

- **넣을 곳** `data/median-income.json` → `propertyConversion` (현재 `null`)
- **지금 상태** 확인하지 못해 의도적으로 비워 두었다. 그래서 `assets`는
  제도별 재산 상한과 **직접 비교**만 하고 있다 (예: 국민취업지원제도 `assets ≤ 4억`).
- **받아야 할 값**
  - 기본재산액 공제 (대도시 / 중소도시 / 농어촌 각각)
  - 주거용재산 한도액
  - 소득환산율 (주거용 / 일반재산 / 금융재산 / 자동차) — 월 %
  - 부채 차감 방식
- **반영 후 할 일** `internal/income/` 에 환산 함수를 추가하고, `WithIncomePct`가
  `소득평가액 + 재산의 소득환산액`을 쓰도록 바꾼다. **화면 계약은 바뀌지 않는다**
  (`householdIncomePct` 하나로 계속 나간다).

### A-3. 소득평가액 공제

- **넣을 곳** 새 파일 `data/income-deduction.json` (아직 없음)
- **지금 상태** `incomeMonthly`를 그대로 중위소득과 비교한다. 공제를 반영하지 않으므로
  **실제보다 소득이 높게 잡힌다** — 오늘 시연에서 월 256만원이 61.0%로 나와
  60% 기준을 아슬하게 넘어 미해당이 된 것이 그 예다.
- **받아야 할 값**
  - 근로소득 공제율 (30%? 대상 범위?)
  - 청년·장애인·한부모 추가 공제
  - 공적이전소득 중 소득에서 빼는 항목
- **반영 후 할 일** 이 값이 없는 동안은 미해당으로 단정하지 않도록,
  기준선 ±5% 안에 드는 경우를 `NEEDS_INFO`로 돌리는 규칙을 검토한다 (3절 C 참조).

### A-4. 가구 범위

- **넣을 곳** 판정 로직이 아니라 **입력 화면의 안내 문구**와 `FE/components/ManualForm.tsx`의
  `가구원 수` 도움말
- **받아야 할 값** 주민등록 기준인지 생계 기준인지, 별거 배우자·군복무·시설 입소자 처리
- **왜 중요한가** 가구원 수 하나가 틀리면 중위소득 기준선 전체가 틀어진다.
  지금 도움말은 "본인 포함" 한 줄뿐이다.

### B-3. 중복수급 관계

- **넣을 곳** `data/relations.json` (로더는 이미 읽을 준비가 되어 있다)
- **지금 상태** 관계 데이터가 **0건**이다. `ResolveConflicts`는 동작하지만 할 일이 없다.
- **받아야 할 값** 실제로 배타 관계인 조합. 특히
  - 생계급여 ↔ 한부모 아동양육비
  - 주거급여 ↔ 청년월세 특별지원
  - 국민취업지원제도 ↔ 실업급여
  - 각각 `EXCLUSIVE` / `REDUCING`(감액률) / `PREREQUISITE` 중 무엇인지
- **형식**
  ```json
  { "from": "...", "to": "...", "type": "EXCLUSIVE", "reason": "..." }
  ```

### C-1. 제도 우선순위

- **넣을 곳** `data/programs/*.json` (현재 **3건**)
- **받아야 할 값** "사각지대에서 가장 많이 놓치는 제도" 순서. 30건을 다 만들 수 없으니
  실제로 도움이 되는 순서가 필요하다.
- **작성 규칙** `data/programs/README.md` 참조. `source.revisedAt`과
  `source.note`(표현하지 못한 요건)는 반드시 채운다.

---

## 2. 자문과 무관하게 오늘 발견한 것 — 바로 고칠 수 있다

### ★ (가) `none` 그룹 조건이 화면에서 뒤집혀 보인다

**증상** 국민취업지원제도의 `재직 중이 아닐 것` 조건이 결과 화면에 붉은 `FAIL`로 표시된다.
그런데 실제로는 *배제 조건이 성립하지 않았다* = 좋은 상태다.

**원인** 판정 자체는 맞다 (`decide()`는 `none`이 PASS일 때만 탈락시킨다).
문제는 `MatchResult.Conditions`가 **평평한 배열**이라, 프론트가 그 조건이
`all` / `any` / `none` 중 어디 소속인지 알 수 없다는 것이다.

**고칠 곳** `internal/model/result.go` + `internal/rules/engine.go`

**후보 1 — 그룹을 표시한다 (권장)**
```go
type ConditionResult struct {
    Condition Condition       `json:"condition"`
    Group     string          `json:"group"` // "all" | "any" | "none"
    Status    ConditionStatus `json:"status"`
    ...
}
```
프론트가 `group === "none"`이면 PASS/FAIL 표시를 뒤집는다. 원자료를 그대로 두므로
"규칙 엔진이 무엇을 봤는가"가 손상되지 않는다.

**후보 2 — 사용자 관점으로 이미 뒤집어 보낸다**
`none` 조건은 `Status`를 반전해 담는다. 프론트가 단순해지지만,
엔진 내부 판정과 응답의 뜻이 달라져 디버깅이 어려워진다.

→ **후보 1을 권한다.** 프론트 타입(`FE/types/index.ts`)도 같이 고쳐야 한다.

### (나) 사유 문구에 원시 enum 이 남는다

**증상** `"EMPLOYED 이(가) 아닙니다 (입력: LOST_JOB)"`

**상태** 프론트는 `입력값` / `기준` 열을 이미 한글로 바꿔 보여준다(`FE/lib/format.ts`).
남은 것은 백엔드가 만드는 `reason` 문자열 하나다.

**고칠 곳** `internal/rules/engine.go` 의 사유 생성부.
`Condition.Label`이 있으면 그것을 쓰고, enum 값은 한글 표를 거치게 한다.
표는 `internal/model` 에 두면 프론트와 두 벌이 되지만, 사유 문구는 서버가 만드는
문장이므로 서버가 갖는 게 맞다.

### (다) 데모 함정 — CORS 오리진

**증상** 오늘 실제로 겪었다. 포트 3000이 다른 프로세스에 잡혀 Next 가 3001로 뜨자
백엔드가 **모든 요청을 CORS로 막았다.** 화면에는 "백엔드 없음"만 뜬다.

**원인** `cmd/server/main.go` 의 기본값이 `http://localhost:3000` 하나다 (정확 일치).

**대응** 발표 전 확인 절차를 `DEMO.md`에 추가한다.
```bash
PORT=8081 CORS_ALLOWED_ORIGINS="http://localhost:3000,http://localhost:3001" go run ./cmd/server
```
기본값 자체에 3001을 넣을지는 판단 필요 — 허용 목록을 넓히는 것이므로
개발 환경에 한정하는 편이 낫다.

---

## 3. 자문 답변이 로직을 바꿀 수 있는 항목

### C. 기준선 근처를 어떻게 다룰 것인가

오늘 시연에서 중위소득 **61.0%** 가 나와 60% 기준 제도가 `INELIGIBLE`이 되었다.
그런데 A-3(소득평가액 공제)이 반영되지 않은 숫자다. 공제를 넣으면 60% 아래로
내려갈 수 있다.

**교수님께 확인할 것**
> "공제 전 소득으로 계산한 값이 기준선을 조금 넘을 때, 이용자에게 '미해당'이라고
> 말해도 됩니까, 아니면 '확인 필요'로 두는 게 맞습니까?"

이 답에 따라 `internal/rules/engine.go` 의 `decide()` 가 바뀐다.
설계 원칙 3("애매하면 확인필요")에 비추면 **확인필요**가 맞아 보이지만,
모든 제도를 확인필요로 만들면 결과가 쓸모없어진다. 경계값 폭을 정해야 한다.

---

## 4. 자문 전 확인해 둔 현재 상태

```
제도 데이터        3건   (한부모 아동양육비 · 국민취업지원 I유형 · 청년월세)
중복수급 관계      0건
기준중위소득       2026년 (보건복지부 고시 제2025-135호)
propertyConversion null  ← A-2 로 채운다
소득평가액 공제    미반영 ← A-3 로 채운다
go test ./...      전부 통과
FE npm run build   통과 (/ · /result)
AI                 키 없음 — 판정은 정상 동작 (aiEnabled: false)
```

## 5. 자문 자리에서 꼭 받아 올 것 (우선순위)

1. **B-3 중복수급 관계** — 데이터가 0건이라 기능이 비어 있다. 제품의 차별점인데 지금은 껍데기다
2. **A-3 소득평가액 공제** — 판정 결과가 실제로 틀리고 있다
3. **C-1 제도 30건 우선순위** — 최대 병목
4. **A-2 재산 소득환산** — 없어도 일부 제도는 판정된다
5. **3절 C 경계값 처리** — 원칙에 관한 판단
