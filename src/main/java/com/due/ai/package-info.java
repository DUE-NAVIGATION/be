/**
 * AI 게이트웨이 (Phase 4 / 6).
 *
 * <p>AI 의 역할은 딱 둘이다.
 * <ol>
 *   <li>자연어 → 구조화된 UserContext</li>
 *   <li>판정 결과 → 사람 말 설명</li>
 * </ol>
 *
 * <p>★ "이 사람이 이 제도에 해당하나요?" 를 LLM 에 묻는 코드를 작성하지 마라.
 * <p>★ 전송 전 시크릿 필터: 주민번호·계좌번호 패턴 제거. 이름·주소는 보내지 않는다.
 */
package com.due.ai;
