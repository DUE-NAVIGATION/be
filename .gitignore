# 빌드 산출물
/server
/validate
/seed
*.exe
/bin/

# 제도 DB — 생성물이다. 원본은 data/programs/*.json 이고
# `go run ./cmd/seed` 로 언제든 다시 만든다. 커밋하면 JSON 과 어긋난다
data/*.db
data/*.db-wal
data/*.db-shm

# 환경변수 — .env.example 만 커밋한다
.env
!.env.example

# 테스트·커버리지
*.out
coverage.html

# 에디터
.idea/
.vscode/
.DS_Store
