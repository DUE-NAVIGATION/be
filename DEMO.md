# 데모 운영 안내

발표에서 데모가 깨지면 나머지가 전부 무의미하다. 이 문서는 **그 일이 일어나지 않게** 하는 절차다.

## 핵심 — API 키도 네트워크도 필요 없다

```bash
DEMO_MODE=true go run ./cmd/server
```

이 한 줄이면 `/api/extract` 와 `/api/explain` 이 **미리 뽑아둔 응답**(`data/demo-cache.json`)으로 동작한다.
`/api/evaluate` 는 원래부터 AI 를 쓰지 않으므로 전 구간이 **외부 호출 0건**으로 완주된다.

> 현장 와이파이가 죽어도, API 키가 만료돼도, 요금이 떨어져도 데모는 돌아간다.

시작 로그에서 이 줄을 확인할 것.

```
msg="데모 모드입니다. AI 응답은 캐시에서 나갑니다 (API 호출 없음)"
```

---

## 리허설에 쓸 입력 문장

`data/demo-cache.json` 에 들어 있는 문장이다. **이 문장 그대로 입력해야** 미리 준비한 응답이 나온다.

| # | 문장 | 나오는 것 |
| --- | --- | --- |
| 1 | `혼자 애 키우는데 일이 끊겼어요. 아이는 7살이고 월세 살아요.` | 가구원 2 · 한부모 · 자녀 7세 · 실직 · 월세 + **되묻기 2개** |
| 2 | `혼자 애 키우는데 일이 끊겼어요. 아이는 7살이고 월세 살아요. 월 소득은 80만원 정도예요.` | 위 + 월 소득 80만원 → **해당 1건 · 확인필요 2건 · 연 276만원** |
| 3 | `스물아홉인데 혼자 살고 월세 내고 있어요. 월급은 120만원이에요.` | 1인 가구 청년 시나리오 |

**1번 → 되묻기 → 2번** 순서가 기획서의 데모 흐름이다. "부족한 정보를 되묻는다" 장면을 보여줄 수 있다.

### 문장을 틀리게 입력하면

기본값(`*`)으로 내려간다. **값을 지어내지 않고 되묻기만** 돌려주므로 화면이 깨지지는 않는다.
서버 로그에 경고가 남는다.

```
msg="데모 캐시: 정확히 맞는 항목이 없어 기본 항목을 씁니다" op=extract
```

리허설 중 이 경고가 보이면 **입력 문장이 캐시와 다른 것**이다. 문장을 맞추거나 캐시에 추가할 것.

### 캐시에 문장 추가하기

`data/demo-cache.json` 의 `entries.extract` 에 항목을 넣으면 된다.
키는 **입력 문장 그대로** 적으면 된다 — 서버가 공백을 정리해 맞춘다.

---

## 발표 전 체크리스트

### 하루 전

- [ ] `go test ./... -count=1` 전부 통과
- [ ] `go run ./cmd/validate` — 제도 데이터 통과
- [ ] `DEMO_MODE=true` 로 띄우고 **3분 시나리오 전체 완주** (아래 흐름)
- [ ] 리허설 **최소 3회**, 타이머 켜고
- [ ] `go mod download` 를 미리 돌려 **모듈 캐시를 채워둔다.** 현장 와이파이에서
      의존성을 받다 막히면 데모가 끝난다 (외부 의존성: modernc.org/sqlite)
- [ ] 발표 노트북에서 직접 확인 (다른 PC 에서 되는 것은 근거가 안 된다)

### 당일 아침

- [ ] 서버 起動 후 `curl http://localhost:8080/healthz` → `"status":"ok"`
- [ ] ★ **포트가 밀리지 않았는지.** 3000 이 다른 프로세스에 잡히면 Next 가 3001 로
      뜨는데, `CORS_ALLOWED_ORIGINS` 기본값은 `http://localhost:3000` **하나뿐**이라
      정확 일치에서 걸려 **모든 요청이 막힌다.** 화면에는 "백엔드 없음"만 보여
      원인을 찾기 어렵다. 실제로 개발 중에 당했다.
      ```bash
      # 프론트가 뜬 포트를 확인한 뒤 그 오리진을 넣어 띄운다
      CORS_ALLOWED_ORIGINS="http://localhost:3000,http://localhost:3001" go run ./cmd/server
      ```
      프론트의 `.env.local` 의 `NEXT_PUBLIC_API_BASE_URL` 도 서버 `PORT` 와 맞출 것
- [ ] 시작 로그에 **"데모 모드입니다"** 가 있는지
- [ ] `programCount` 가 기대한 수인지
- [ ] 프론트에서 결과 화면까지 한 번 완주
- [ ] **새로고침 시연** 이 실제로 동작하는지 (입력이 사라지는지)
- [ ] 화면 해상도 1920×1080 에서 글자가 충분히 큰지
- [ ] 프로젝터는 대비가 낮게 나온다 — 실제 프로젝터로 한 번

### 발표 직전

- [ ] 브라우저 탭 정리, 알림 끄기
- [ ] 터미널 글자 크기 키우기 (로그를 보여줄 경우)
- [ ] 서버 재시작해서 깨끗한 상태로

---

## 3분 시나리오 (2026-09-14 — 시설 연결 중심)

AI 키 없이 직접 입력으로 진행해도 완주된다. 판정 결과는 똑같다.

| 시각 | 장면 | 할 것 · 할 말 |
| --- | --- | --- |
| 00:00 | 문제 제기 | "한국 복지는 신청주의입니다. 알아도 **어디에 물어야 할지** 모르면 닿지 못합니다." |
| 00:20 | 첫 화면 `/` | 세 걸음 · 공공과 민간 · "저장하지 않습니다" 를 짧게 짚는다 |
| 00:35 | 상황 입력 `/start` | `서울특별시` · `관악구` · 한부모 `예` · 자녀 나이 `7` → **받을 수 있는 것 찾기** |
| 00:55 | **결과 — 공공 기관** ★ | 상담전화와 관악구정신건강복지센터. **"왜 이렇게 판정됐나요"** 를 펼쳐 조건별 근거를 보여준다 |
| 01:25 | **구분선 → 민간 기관** ★ | "여기부터 민간 기관" — 관악구 지역아동센터. "공공은 대부분 무료, 민간은 정원·이용료를 먼저 물어보세요" |
| 01:50 | **연결** ★ | 한 곳에서 **전화하기 전에 볼 것** → 챙겨갈 것 + "이렇게 말하시면 됩니다" 문구. "이 문구는 이 기기에서만 만들어집니다" |
| 02:15 | 지원금 | 아래로 내려 제도 판정. "시설에 전화할 때 함께 물어보시면 됩니다" |
| 02:35 | **개인정보** ★ | "저희는 아무것도 저장하지 않습니다." → **실제로 새로고침** → "결과가 사라졌습니다. 그렇게 만들었습니다." |
| 02:50 | 마무리 | "제도도, 기관도 이미 있습니다. 닿지 못했을 뿐입니다." |

> 시간이 모자라면 00:20 과 02:15 를 뺀다. **공공/민간 결과와 연결 장면**이 이 프로젝트의 승부처다.

### 받을 질문

- **"GPT 에 물어보면 되지 않나요?"** — 판정을 AI 가 하지 않습니다. 조건 하나하나를 규칙으로 대조하고 근거를 보여줍니다. 번호를 지어내지도 않습니다
- **"민간 기관이 적지 않나요?"** — 지금은 서울 지역아동센터만 넣었습니다. 공공데이터 CSV 를 임포터로 넣는 구조라, 다른 종류도 같은 방법으로 늘어납니다
- **"예약까지 되나요?"** — 기관 쪽에 예약 API 가 없습니다. 그래서 전화·문자·지도와 할 말 준비까지 합니다


---

## 배포

```bash
docker build -t due-api .
docker run -p 8080:8080 -e DEMO_MODE=true due-api
```

- 빌드 중 **제도 JSON 검증이 자동으로 돈다.** 깨진 제도가 있으면 이미지가 만들어지지 않는다
- 최종 이미지 약 **17MB** (distroless · 셸 없음 · nonroot)
- 배포하면 `CORS_ALLOWED_ORIGINS` 에 **프론트 배포 도메인을 반드시 추가**할 것.
  안 하면 브라우저가 응답을 못 읽는데, 서버 로그에는 200 만 찍혀서 원인을 찾기 어렵다

```bash
docker run -p 8080:8080 \
  -e DEMO_MODE=true \
  -e CORS_ALLOWED_ORIGINS=https://due.vercel.app,http://localhost:3000 \
  due-api
```

---

## 무엇이 깨져도 무엇은 살아 있는가

| 깨진 것 | 살아 있는 것 |
| --- | --- |
| 네트워크 · API 키 | **전부** (DEMO_MODE) |
| 제도 JSON 하나 | 나머지 제도로 판정. 서버는 죽지 않는다 |
| AI 캐시 파일 | 판정(`/api/evaluate`)은 정상. 대화형 입력만 막힘 |
| 기준중위소득 표 | **서버가 뜨지 않는다** — 이건 없으면 판정 자체가 불가능하다 |

마지막 줄이 유일한 단일 실패 지점이다. `data/median-income.json` 을 지우지 말 것.
